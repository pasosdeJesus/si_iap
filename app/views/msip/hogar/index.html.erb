<% if !File.directory?(Msip.ruta_anexos) %>
	<div class="alert fade in alert-danger ">
		<button type="button" class="close" data-dismiss="alert">×</button>
		ATENCIÓN: No existe ruta de anexos <%= Msip.ruta_anexos %>
	</div>
<% end %>
<article>
<br>
<center><img src="<%= Rails.configuration.relative_url_root %>/images/logo.jpg" class="img-responsive"></img></center>

<%= render "local" %>
</article>
